[project]
name = "d_back"
version = "0.0.1"
description = "WebSocket server project."
authors = ["Tin Nguyen <nguyen.ngoctindaniel@gmail.com>"]
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
